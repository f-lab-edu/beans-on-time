spring:
  application:
    name: beans-on-time
  docker:
    compose:
      enabled: true
      file: compose.yaml
      lifecycle-management: start_only
      skip:
        in-tests: true
logging:
  level:
    org.springframework.security: TRACE
